import { createStitches } from "@stitches/react";

export const {
  config,
  styled,
  css,
  getCssText,
  globalCss,
  theme,
  createTheme,
} = createStitches({
  theme: {
    colors: {
      primary: "#ff0000",
    },
  },
});
